on:
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  run-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout & install Dependancies
        uses: ./.github/actions/checkout_install.yml
      - name: Build
        run: npm run build
  run-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout & install Dependancies
        uses: ./.github/actions/checkout_install.yml
      - name: Lint
        run: npm run lint
